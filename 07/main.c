#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a,b,max,addition,difference,multiplication,divided;
    printf("Enter Your Two Numbers ");
    addition=a+b;
    printf("Sum of your numbers %d",addition);
    if (a>b)
    difference=a-b;
    else
    difference=b-a;
    printf("Difference %d",difference);
    multiplication=a*b;
    printf("Multiplication %d",multiplication);
    if (a>b)
    max=a;
    else
    max=b;
    printf("The highest number is &d",max);
    divided=a/b;


    return 0;
}
